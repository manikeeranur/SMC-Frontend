// ─── Trading Constants ─────────────────────────────────────────────────────────
// Edit LOT_SIZE here — used everywhere in the UI (P&L calculations, labels, display)
export const LOT_SIZE = 65; // NIFTY lot size (1 lot = 65 shares)
